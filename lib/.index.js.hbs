/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 */

'use strict'

const sgStorage = require('./sg_storage')

let lib = sgStorage.bind(this)

Object.assign(lib, sgStorage, {

})

module.exports = lib
